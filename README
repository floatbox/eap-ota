eviterra.com
============

На Ubuntu требуется выполнить:

    sudo apt-get install libcurl3-dev libmysqlclient-dev

delayed_job
-----------

дебажим примерно так:

    bundle exec rails runner 'puts Delayed::Job.last.last_error'
