Payment:
  fields:
    default: ref, price, charged_at, name_in_card, last_digits_in_card, reject_reason
    list: ref, price, created_at, charged_at, name_in_card, last_digits_in_card, payment_status, reject_reason, system
    show: ref, price, created_at, charged_at, payture_state, last_digits_in_card, name_in_card, reject_reason, order, system
    edit: price, charged_at
    options:
      date_formats:
        created_at: long
  options:
    default_action_on_item: show
  search: =ref, name_in_card
  relationships: order
  order_by: -created_at
  filters: created_at
  application: Misc, 2
