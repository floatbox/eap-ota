<h2>Партнеры</h2>

<table class="pData">
  <tr valign="top">
    <th>поисков</th><th>переходов</th><th>% успешных</th><th>поиск/переход</th><th>заказов</th><th>заказ/переход</th><th>оборот</th><th>% от оборота</th><th>доход</th><th>% от дохода</th><th>марж.</th>
  </tr>
  <% report.each do |name, partner|%>
  <tr><td colspan="7"><h3><%= name.capitalize%></h3></td></tr>
  <tr>
    <td><%= display_numbers(partner[:search])%></td>
    <td><%= display_numbers(partner[:enter])%>/<%= display_numbers(partner[:enter_success])%></td>
    <td><%= display_percent(partner[:successes_per_enter])%></td>
    <td><%= display_percent(partner[:enters_per_search])%></td>
    <td><%= display_numbers(partner[:order_count])%></td>
    <td><%= display_percent(partner[:conv])%><%= display_percent(partner[:conv_success])%></td>
    <td><%= display_price(partner[:order_total])%></td>
    <td><%= display_percent(partner[:order_total_share])%></td>
    <td><%= display_price(partner[:income_total])%></td>
    <td><%= display_percent(partner[:income_share])%></td>
    <td><%= display_percent(partner[:markup])%></td>
  </tr>
  <% end %>
</table>
