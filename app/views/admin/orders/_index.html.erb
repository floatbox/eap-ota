<script type="text/javascript">
 jQuery(document).ready(function() {
   jQuery.timeago.settings.allowFuture = true;
   jQuery("abbr.timeago").timeago();
 });
</script>

<script type="text/javascript">
jQuery(function($) {
//  $("#sidebar").append($(".predefined_filters").html());
  $(".predefined_filters").first().clone().appendTo('#sidebar').wrap('<ul />');
  $("#sidebar .predefined_filters").css({border: '1px solid lightGrey', marginTop: '25px', lineHeight: '1.4', fontSize: '1.1em', color: '#444'});
  $("#sidebar .predefined_filters li").css({display: 'block'});
});
</script>


<%= render 'admin/resources/index' %>
(в выборке <%= @resource.count %> записей;
всего <%= number_to_currency @resource.sum(:price_with_payment_commission), :locale => :ru %> /
в среднем <%= number_to_currency @resource.average(:price_with_payment_commission), :locale => :ru %>)
<br />
Доход(income): <%= number_to_currency @resource.sum(:stored_income), :locale => :ru %>
