<%= render 'admin/resources/index' %>
(в выборке <%= @resource.count %> записей;
Уплачено:
всего <%= human_price @resource.sum(:price) %> /
в среднем <%= human_price @resource.average(:price) %>;
Поступлений:
всего <%= human_price @resource.sum(:earnings) %> /
в среднем <%= human_price @resource.average(:earnings) %>)
