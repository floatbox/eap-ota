<%= render 'admin/resources/index' %>
(в выборке <%= @resource.count %> записей;
Уплачено:
всего <%= number_to_currency @resource.sum(:price) %> /
в среднем <%= number_to_currency @resource.average(:price) %>;
Поступлений:
всего <%= number_to_currency @resource.sum(:earnings) %> /
в среднем <%= number_to_currency @resource.average(:earnings) %>)
